Notes: Dependencies needed to run this locally are in requirements.txt

-> First run "pip install -r requirements.txt" before doing anything

Also, if using windows, modify run_local_sqlite.sh to use "python" instead of "python3" in the command